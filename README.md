# Расчёт нагрузки на сеть провайдера

Учебный проект по дисциплине «Разработка интернет-приложений» (РИП).

## Предметная область

Система описывает сеть маршрутизаторов провайдера: центральные, промежуточные и конечные узлы жилых домов. Основная сущность — маршрутизатор. В дальнейших работах заявка будет рассчитывать нагрузку для маршрутизаторов в топологии «звезда/снежинка», где у каждого нижестоящего узла указан один мастер-маршрутизатор.

## Технологии

- Go;
- серверная HTML-шаблонизация (SSR);
- PostgreSQL, Adminer и MinIO в Docker — на этапе лабораторной работы №2.

## Лабораторные работы

| Ветка | Содержание |
| --- | --- |
| [`ssr-design`](../../tree/ssr-design) | ЛР1: дизайн, SSR, in-memory коллекция маршрутизаторов и MinIO. |
| [`database-orm`](../../tree/database-orm) | ЛР2: PostgreSQL, Adminer, ORM, миграции и логическое удаление. |
| `rest-api` | Планируемая ЛР3: веб-сервис. |
| `auth-sessions` | Планируемая ЛР4: аутентификация и сессии. |

ФИО и учебная группа в исходных материалах не указаны, поэтому намеренно не добавлены.
